{
  "id": "ship-tls-rejunauth-1",
  "ruleKey": "tls-security:js-reject-unauthorized",
  "module": "tlsSecurity",
  "pattern": "rejectUnauthorized\\s*:\\s*false",
  "transform": {
    "kind": "regex-replace",
    "find": "rejectUnauthorized(\\s*):(\\s*)false",
    "replace": "rejectUnauthorized$1:$2true",
    "flags": "g"
  },
  "description": "Flip rejectUnauthorized: false to true (re-enable TLS validation). Mirrors the ast-reject-unauthorized AST transform so non-AST-parseable contexts (template strings, JSON config dumps, shell heredocs) still get the fix.",
  "schemaVersion": 1,
  "origin": "curated"
}
