{
  "id": "ship-var-to-const-1",
  "ruleKey": "lint:prefer-const",
  "module": "lint",
  "pattern": "^[ \\t]*var\\s+[A-Za-z_$]",
  "transform": {
    "kind": "regex-replace",
    "find": "^([ \\t]*)var(\\s+[A-Za-z_$])",
    "replace": "$1const$2",
    "flags": "gm"
  },
  "description": "Replace top-level `var` declarations with `const`. Conservative: only matches indent+var+identifier at line start (no destructuring), to avoid the let-is-the-right-choice cases. Mirrors ast-var-to-const.",
  "schemaVersion": 1,
  "origin": "curated"
}
